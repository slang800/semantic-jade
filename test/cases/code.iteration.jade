-items = [1,2,3]

ul
	-for item in items
		li #{item}

ul
	-for item, i in items
		li(class="item-#{i}") #{item}

-nums = [1, 2, 3]
-letters = ['a', 'b', 'c']

ul
	-for l in letters
		-for n in nums
			li #{n}: #{l}